# Default values for cluster_autoscaler.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  repository: k8s.gcr.io/autoscaling/cluster-autoscaler
  tag: "v1.22.2"

autoscaler_role: 
cluster_name: 